
export const PROMPT_TOOLS_TOOLTIP =
'Pass any valid JSON for your provider\'s tool format. Each provider (OpenAI, Anthropic, Bedrock, Google Vertex AI, etc.) expects a different schema. We pass your JSON through as-is—if the API returns an error, it will appear in the Output panel for debugging.';

export const PROMPT_RESPONSE_FORMAT_TOOLTIP =
'Pass any valid JSON for your provider\'s structured output format. Each provider expects a different schema. We pass your JSON through as-is—if the API returns an error, it will appear in the Output panel for debugging.';